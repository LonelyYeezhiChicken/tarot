# Tasks: Add Card Name to UI

1.  [x] **Task 1: Update card rendering logic:**
    -   Modify the `handleCardClick` function in `src/main.js` to add an element for the card name.
2.  [x] **Task 2: Style the card name:**
    -   Add CSS rules in `public/style.css` to style the card name text.
3.  [x] **Task 3: Validation:**
    -   Manually verify that the card name appears correctly on the card when flipped.